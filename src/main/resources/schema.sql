CREATE TABLE sex (
                       id BIGINT AUTO_INCREMENT PRIMARY KEY,
                       title VARCHAR(255),
                       file BLOB,
                       writer VARCHAR(255),
                       publish DATE
);

# CREATE TABLE IF NOT EXISTS books (
#                                      id BIGINT AUTO_INCREMENT PRIMARY KEY,
#                                      title VARCHAR(255),
#                                      file BLOB,
#                                      writer VARCHAR(255),
#                                      publish DATE
# );

